name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  node:
    uses: CosmicBytez/.github/.github/workflows/ci-node.yml@v1
    with:
      node-version: "22"
      test-command: npm test -- --coverage --ci
